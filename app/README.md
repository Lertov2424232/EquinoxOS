These are pre-installed apps in EquinoxOS that made with testing purpoces.


### HTML-View: 
> Now its NOT only a HTML file viewer, but a browser. Yet it can only open HTTP sites, through the system doesnt have the encryption.

### NiPlay:
> Musical Player for .WAV files. Preinstalled tracks here are: terry.wav (TempleOS "God's Music") and middan.wav (My own track that i've made) [Now it's deleted]
Example:

```bash
run niplay.elf terry.wav
```


### LuaGUI:
> Lua scripts runner into external app. Very cutted Lua, but yet it's working
Example:

```bash
run luagui.elf app.lua
```

### BMPView:
> Program made to open BMP images. Through very buggy.

```bash
run bmpview.elf BG.BMP
```

### Snake
> Classic Snake game.